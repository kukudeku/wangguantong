# 网管通电竞网吧运营管理系统需求规格说明书

## 文档信息

| 项目 | 内容 |
|---|---|
| 文档版本 | V2.1 |
| 系统基线 | 2026-07-15 当前主系统 |
| 适用范围 | 用户自助端、管理员后台、Spring Boot 后端、MySQL 数据库 |
| 运行基线 | 前端 `5173`、后端 `8087`、18 张表、64 个接口 |
| 文档状态 | 与当前实现同步 |

## 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| V1.0 | 2026-07-13 | 初版，覆盖会员、机位、上机、预约和基础点餐 |
| V2.0 | 2026-07-14 | 增补真实支付、优惠券签到、团购券、推广邀请、统一服务工单，并修正接口、表和部署基线 |
| V2.1 | 2026-07-15 | 增补用户端微信/支付宝余额充值支付、支付业务类型与到账幂等规则，接口数同步为 64 |

## 1. 项目概述

### 1.1 建设目标

网管通用于把顾客自助服务与网吧后台运营连接到同一套业务数据中，目标包括：

1. 统一管理会员、机位、上机、预约、点餐、余额和营销数据；
2. 形成“上机—实时计费—余额不足自动下机—释放机位”闭环；
3. 形成“购物车—优惠券—支付—订单交付—取消退款”闭环；
4. 支持签到优惠券、邀请码奖励和团购券核销；
5. 支持呼叫网管、故障报修和后台工单处理；
6. 支持本地开发和宝塔/Nginx 部署。

### 1.2 系统定位与边界

系统定位为课程设计与业务原型，可用于受控环境演示。当前具备真实微信/支付宝支付接入代码，但平台默认关闭，实际收款需要商户资质、HTTPS 回调和服务器密钥。

当前未实现：

- Spring Security、服务端 Session/JWT 和细粒度接口权限；
- 密码散列、短信验证码和联网实名认证；
- 多门店、库存采购、交接班、发票和完整财务对账；
- 电脑客户端远程开关机、锁屏；
- Redis、消息队列、WebSocket 和高并发分布式控制。

### 1.3 技术与运行约束

| 层级 | 当前技术 |
|---|---|
| 前端 | Vue 3、Vue Router、Axios、Vite 5、Arco Design Vue |
| 后端 | Java 17、Spring Boot 3.3.5、MyBatis-Plus 3.5.7 |
| 数据库 | MySQL 8、`utf8mb4`、金额 `DECIMAL` |
| 本地地址 | 前端 `http://localhost:5173`，后端 `http://localhost:8087` |
| 接口约定 | 统一返回 `code`、`message`、`data` |
| 登录状态 | `localStorage.user` 与 `localStorage.admin` 相互独立 |

## 2. 角色与入口

| 角色 | 入口 | 主要权限 |
|---|---|---|
| 未登录访客 | `/`、`/user/login` | 注册、用户登录 |
| 正常用户 | `/user` | 上机、预约、点餐、营销活动、服务工单、账户查询 |
| 管理员 | `/admin` | 运营管理、基础资料、工单、营销规则和订单处理 |

用户按 `memberLevel` 分为普通会员、黄金会员、钻石会员；上机折扣分别为 100%、90%、80%。`userType` 仍保留“会员/散客”，散客充值后自动转为普通会员。点餐后台可为散客创建订单，但散客不能使用余额支付。

## 3. 功能结构

### 3.1 用户端

```text
用户自助端
├── 上机服务：机位大厅、当前上机、我的预约、上机记录
├── 点餐服务：商品选购、我的订单
├── 会员活动：每日签到、我的优惠券
├── 推广计划：邀请好友、邀请记录
├── 服务中心：呼叫网管、故障报修、服务记录
└── 账户服务：余额账户、团购验券、修改密码、退出
```

### 3.2 管理员后台

```text
管理员后台
├── 数据概览：首页看板
├── 会员服务：会员列表、余额充值
├── 机位管理：电脑列表
├── 服务中心：服务工单
├── 上机运营：预约管理、上机管理
├── 点餐管理：点餐订单、商品维护
└── 营销活动：团购券、优惠券模板、签到奖励规则、推广计划
```

## 4. 功能需求

### 4.1 认证与账户

认证模块为用户端和管理员端提供各自独立的入口。用户使用身份证号作为登录账号，注册时由后端校验身份证号和手机号唯一性，并生成个人邀请码；管理员则使用后台账号登录。当前登录状态主要保存在浏览器本地，因此这些功能适合课程演示，公开部署前仍需补充服务端身份认证和接口授权。

| 编号 | 需求 |
|---|---|
| FR-AUTH-001 | 根路径跳转 `/user`；未登录访问用户端时进入 `/user/login`。 |
| FR-AUTH-002 | `/admin` 跳转 `/admin/dashboard`；未登录进入 `/admin/login`。 |
| FR-AUTH-003 | 用户使用身份证号和密码登录，禁用用户不得登录。 |
| FR-AUTH-004 | 注册必填姓名、身份证号、手机号、密码；身份证号和手机号必须唯一。 |
| FR-AUTH-005 | 注册可填写邀请码；有效且活动启用时完成双方奖励。 |
| FR-AUTH-006 | 新用户默认为普通会员、正常状态、余额 0，并生成唯一邀请码。 |
| FR-AUTH-007 | 修改密码必须校验原密码；用户端和管理员端分别退出。 |

### 4.2 会员与余额

会员资料是上机、点餐、充值和营销业务的共同基础。后台人工充值会直接在事务中更新余额；用户端微信或支付宝充值则必须先生成第三方支付单，只有验签回调或主动查单确认成功后才到账。充值记录会保存资金来源关联单号，确保同一支付结果不会重复增加余额。

| 编号 | 需求 |
|---|---|
| FR-MEMBER-001 | 后台按姓名、身份证号、手机号查询会员。 |
| FR-MEMBER-002 | 后台可新增、编辑、删除会员，并维护余额、等级、类型和状态。 |
| FR-MEMBER-003 | 新增/修改身份证号时同步登录账号；编辑时密码留空保留旧密码。 |
| FR-MEMBER-004 | 后台人工充值校验金额大于 0，并在事务中更新余额、写充值记录。 |
| FR-MEMBER-005 | 散客首次充值后自动转为普通会员。 |
| FR-MEMBER-006 | 用户可查看充值、点餐和上机合并后的余额明细。 |
| FR-MEMBER-007 | 用户端余额充值仅支持微信或支付宝；单次金额大于 0 且不超过 10000 元。 |
| FR-MEMBER-008 | 用户端发起充值后先生成第三方支付单，只有验签回调或主动查单确认成功才增加余额并写入关联商户单号的充值记录。 |
| FR-MEMBER-009 | 同一充值支付单重复回调或重复查单不得重复到账。 |

### 4.3 电脑、上机与预约

电脑状态不是单纯的基础资料，而是上机、预约和故障工单共同维护的资源状态。开始上机时系统会校验会员、余额和机位状态，并立即扣除首小时费用；预约会在限定时间内锁定机位，取消、超时或转上机时再同步释放或切换状态。

| 编号 | 需求 |
|---|---|
| FR-ONLINE-001 | 电脑支持空闲、使用中、预约锁定、维修中四种状态。 |
| FR-ONLINE-002 | 只有正常会员、无进行中记录、目标电脑空闲且余额足够时才能上机。 |
| FR-ONLINE-003 | 开始上机立即按会员折扣扣除首个计费小时。 |
| FR-ONLINE-004 | 后端每 60 秒按当前计费小时补扣新增差额；不足一小时按一小时。 |
| FR-ONLINE-005 | 余额不足以支付新增小时自动结束记录并释放电脑。 |
| FR-ONLINE-006 | 用户和管理员可主动下机；历史记录保留消费与机位快照。 |
| FR-ONLINE-007 | 当前版本不提供换机接口；历史兼容字段不代表页面具备换机功能。 |
| FR-RESERVE-001 | 用户只能预约当前至未来 1 小时内的空闲电脑。 |
| FR-RESERVE-002 | 同一会员同一时间只允许一条有效预约。 |
| FR-RESERVE-003 | 预约成功后电脑变为预约锁定；取消、超时后恢复空闲。 |
| FR-RESERVE-004 | 预约上机必须校验记录归属与有效期，并复用普通上机计费。 |

### 4.4 点餐、优惠券与支付

点餐结算由后端重新读取商品价格并计算金额，不能直接采用前端传入的总价。优惠券在满足门槛后按订单明细分摊，余额支付立即扣款，微信和支付宝订单则先进入待支付状态。平台确认成功后订单才可交付，取消已支付订单时按明细实付金额处理退款。

| 编号 | 需求 |
|---|---|
| FR-FOOD-001 | 后台维护商品名称、分类、价格、上下架状态和备注。 |
| FR-FOOD-002 | 用户端只展示上架商品，购物车可批量提交多个商品。 |
| FR-FOOD-003 | 后端按数据库商品价格计算优惠前金额和实付金额，不信任前端金额。 |
| FR-FOOD-004 | 会员可选可使用且未过期的优惠券，满足门槛后抵扣；重复提交不得重复使用。 |
| FR-PAY-001 | 余额支付立即扣余额并把订单置为“已下单”。 |
| FR-PAY-002 | 微信支付使用 API v3 Native 二维码；支付宝使用电脑网站支付页面。 |
| FR-PAY-003 | 第三方订单先为“待支付”，验签成功的异步通知才转为“已下单”。 |
| FR-PAY-004 | 默认 15 分钟未支付自动关闭，并归还订单占用的优惠券。 |
| FR-PAY-005 | 已下单订单可完成；已完成订单不可取消。 |
| FR-PAY-006 | 余额订单取消时退回该明细实付金额；第三方订单按明细原路退款。 |
| FR-PAY-007 | 支付、退款通知必须验签，重复通知不得重复入账或重复退款。 |
| FR-PAY-008 | 微信、支付宝未启用时返回明确未配置提示，不能影响余额支付。 |
| FR-PAY-009 | `payment_record.business_type` 区分“点餐”和“余额充值”，两类业务共用支付验签、主动查单和超时关闭能力。 |

订单业务状态包括：`待支付`、`已下单`、`退款中`、`已完成`、`已取消`。支付主记录状态包括：`待支付`、`已支付`、`已关闭`、`部分退款`、`退款中`、`已退款`。

### 4.5 每日签到与优惠券

签到功能把后台配置的优惠券模板和连续签到规则连接到用户权益。系统按自然日记录签到，自动计算连续天数，并在命中启用规则时发放带领取时间、过期时间和来源记录的用户优惠券；同一用户同一天不能重复签到或重复领券。

| 编号 | 需求 |
|---|---|
| FR-COUPON-001 | 后台维护优惠券名称、使用门槛、优惠金额、有效天数和启停状态。 |
| FR-COUPON-002 | 后台按连续签到天数配置奖励，同一天数只能存在一条规则。 |
| FR-COUPON-003 | 用户同一自然日只能签到一次；断签后连续天数从 1 重新开始。 |
| FR-COUPON-004 | 达到奖励天数时自动发放优惠券，并保存来源、领取时间和过期时间。 |
| FR-COUPON-005 | 优惠券状态为可使用、已使用或已过期；查询时自动标记到期券。 |
| FR-COUPON-006 | 未支付订单关闭或整个批次取消时，应归还已占用优惠券。 |

### 4.6 团购券与推广计划

团购券和邀请推广都属于余额营销，但两者的资金来源不同。团购券由后台预先录入，用户核销后按券面金额充值；推广奖励则在新用户注册并使用有效邀请码时发放给双方。系统通过条件更新和唯一约束防止券码重复核销或新用户重复领奖。

| 编号 | 需求 |
|---|---|
| FR-VOUCHER-001 | 后台录入唯一券码、充值金额、有效期和备注。 |
| FR-VOUCHER-002 | 用户仅能核销后台存在、未使用、未禁用且未过期的券码。 |
| FR-VOUCHER-003 | 核销采用条件更新防止重复使用，并写入充值记录和券码关联。 |
| FR-VOUCHER-004 | 已使用团购券不可重新启用或禁用。 |
| FR-PROMO-001 | 每个会员拥有唯一邀请码，旧会员升级时自动生成 `WG` 加六位编号格式。 |
| FR-PROMO-002 | 注册填写有效邀请码且活动启用时，邀请人和新用户分别获得配置金额。 |
| FR-PROMO-003 | 一个新用户只能领取一次邀请奖励，奖励记录保存双方和金额快照。 |
| FR-PROMO-004 | 后台可设置双方奖励金额并启用或停用活动；金额不得小于 0。 |

默认推广规则为邀请人 10 元、新用户 5 元，实际运行以数据库 `promotion_rule` 为准。

### 4.7 服务中心

服务中心把“呼叫网管”和“故障报修”统一为可追踪工单。呼叫网管可以只描述位置和需求，故障报修必须关联电脑；管理员受理报修后，机位会进入维修中，完成或取消后再恢复可用，从而保证服务处理状态与机位分配状态一致。

| 编号 | 需求 |
|---|---|
| FR-SERVICE-001 | 用户可提交“呼叫网管”或“故障报修”，并查看本人服务记录。 |
| FR-SERVICE-002 | 故障报修必须选择电脑；呼叫网管可填写所在位置而不绑定电脑。 |
| FR-SERVICE-003 | 同一电脑不能同时存在多条待处理/处理中的故障报修。 |
| FR-SERVICE-004 | 工单状态按待处理 → 处理中 → 已完成流转，也可在未结束前取消。 |
| FR-SERVICE-005 | 故障工单受理时电脑变为维修中；完成或取消处理中的报修时恢复空闲。 |
| FR-SERVICE-006 | 使用中或预约锁定电脑不得直接进入维修中，必须先释放机位。 |

### 4.8 后台看板与导航

后台看板用于快速了解当前运营情况，展示会员、电脑、今日充值和今日上机等指标。侧栏按照业务域组织管理入口，帮助管理员从统计概览进入会员、机位、订单、工单和营销规则等具体操作页面。

| 编号 | 需求 |
|---|---|
| FR-ADMIN-001 | 首页显示会员总数、电脑总数、空闲、使用中、今日充值金额、今日上机次数。 |
| FR-ADMIN-002 | 后台按当前菜单分组展示数据概览、会员服务、机位管理、服务中心、上机运营、点餐管理和营销活动。 |
| FR-ADMIN-003 | 侧栏折叠状态保存在浏览器；窄屏自动折叠。 |
| FR-ADMIN-004 | 后台提供进入用户系统和退出登录操作。 |

## 5. 核心业务规则

核心业务规则描述跨页面、跨数据表都必须遵守的约束。它们决定余额和状态何时变化，也是判断重复请求、异常回调或并发操作是否安全的依据。

| 编号 | 规则 |
|---|---|
| BR-001 | 金额使用 `BigDecimal`/`DECIMAL`，不使用浮点数直接保存余额。 |
| BR-002 | 上机按计费小时向上取整，首小时立即扣款，之后只补扣差额。 |
| BR-003 | 会员、电脑和业务记录的联动操作使用事务。 |
| BR-004 | 预约和上机必须以后端当前状态为准，前端展示不构成最终授权。 |
| BR-005 | 一次购物车结算共用一个 `batch_no`，第三方支付共用一条支付主记录。 |
| BR-006 | 优惠金额按订单明细依次分摊，取消明细只退该明细的实付金额。 |
| BR-007 | 第三方支付成功、退款成功以验签后的平台回调或主动查询结果为准。 |
| BR-008 | 定时任务每 60 秒执行上机结算、预约过期和支付超时关闭。 |
| BR-009 | 服务工单、团购券、签到、邀请奖励均必须防止重复提交产生重复业务结果。 |
| BR-010 | 所有列表当前均为不分页查询，适合课程演示数据量。 |

## 6. 数据需求

数据库按业务域拆分为 18 张表，既保存当前状态，也保存订单、上机、充值和奖励等历史记录。金额字段统一使用十进制类型，跨表修改由事务保证整体提交或回滚；当前没有物理外键，关联正确性主要由服务层校验和索引约束维护。

当前完整 SQL 包含 18 张表：

| 域 | 数据表 |
|---|---|
| 账号与机位 | `admin`、`member`、`computer` |
| 上机与预约 | `online_record`、`reservation_record` |
| 余额与团购券 | `recharge_record`、`group_buy_voucher` |
| 点餐与支付 | `food_item`、`food_order`、`payment_record`、`payment_refund` |
| 优惠券签到 | `coupon_template`、`sign_in_reward_rule`、`user_coupon`、`member_sign_in` |
| 推广邀请 | `promotion_rule`、`invitation_record` |
| 服务中心 | `repair_record` |

数据库不声明物理外键，关联一致性主要由 Service 事务和业务校验维护。详细字段见 `数据库设计书.xlsx`。

## 7. 接口需求

接口清单以当前 Controller 映射为准。模块之间存在明确协作关系，例如余额充值会先调用充值模块创建支付单，再通过支付模块完成平台确认；预约、上机和工单则都会影响电脑状态。调用方需要理解业务前置条件，不能只根据请求路径判断操作是否可以执行。

当前 14 个 Controller 共 64 个 HTTP 接口：

| 模块 | 路径前缀 | 接口数 |
|---|---|---:|
| 管理员 | `/admin` | 1 |
| 看板 | `/dashboard` | 1 |
| 会员 | `/member` | 5 |
| 电脑 | `/computer` | 5 |
| 充值 | `/recharge` | 3 |
| 上机 | `/online` | 4 |
| 预约 | `/reservation` | 4 |
| 点餐 | `/food` | 10 |
| 支付 | `/payment` | 6 |
| 用户 | `/user` | 6 |
| 优惠券签到 | `/coupon` | 9 |
| 团购券 | `/voucher` | 4 |
| 推广计划 | `/promotion` | 3 |
| 服务中心 | `/repair` | 3 |

统一业务响应：

```json
{
  "code": 200,
  "message": "成功",
  "data": null
}
```

支付平台通知接口按平台协议返回 HTTP/纯文本结果，不使用通用 `Result` 包装。接口明细见 `API接口设计说明书.docx`。

## 8. 非功能需求与当前风险

### 8.1 可用性

- 用户端和后台均提供明确中文状态与错误提示；
- 高风险操作使用确认框；
- 表格在窄屏可滚动，后台侧栏可折叠；
- 日期在前端统一格式化，避免直接显示 `T`。

### 8.2 可靠性

- 充值到账、上机、预约、点餐、团购券、签到和推广奖励采用事务；
- 支付单号、退款单号、充值关联单号、邀请码、新用户邀请奖励等有唯一约束或幂等校验；
- 定时任务单条失败应记录日志并继续处理其他记录。

### 8.3 安全边界

当前密码明文保存，后端接口没有统一认证授权，用户编号可从请求路径或参数传入；因此系统不能视为生产级安全系统。上线前必须增加密码散列、后端鉴权、接口权限、CSRF/重放防护、限流、审计和敏感数据脱敏。

支付私钥必须通过服务器文件路径加载，不得进入前端、数据库、Git、聊天或截图。生产回调必须使用 HTTPS。

## 9. 验收标准

### 9.1 构建验收

```bash
cd wangguantong-server && mvn test
cd ../wangguantong-web && npm run build
```

两项命令应成功，前端生成 `dist`，后端测试覆盖会员邀请码生成以及支付服务关键分支。

### 9.2 功能验收

| 编号 | 场景 | 预期 |
|---|---|---|
| AC-001 | 正常用户登录 | 进入用户端并显示账户信息 |
| AC-002 | 使用有效邀请码注册 | 新用户创建，双方奖励入账，产生邀请记录 |
| AC-003 | 空闲电脑上机 | 扣首小时、电脑使用中、记录进行中 |
| AC-004 | 余额不足跨小时 | 自动下机并释放电脑 |
| AC-005 | 预约并到期未上机 | 自动取消并释放电脑 |
| AC-006 | 购物车使用优惠券余额支付 | 优惠券占用、余额扣除、订单已下单 |
| AC-007 | 第三方支付未配置 | 返回明确提示，不影响余额支付 |
| AC-008 | 第三方支付成功回调 | 验签后支付已完成，订单变为已下单 |
| AC-009 | 取消已支付明细 | 余额退款或第三方原路退款，防止重复退款 |
| AC-010 | 每日签到达到规则天数 | 自动发放对应优惠券 |
| AC-011 | 团购券重复核销 | 第一次入账，第二次被拒绝 |
| AC-012 | 呼叫网管 | 创建待处理工单，无需绑定电脑 |
| AC-013 | 故障报修受理/完成 | 电脑维修中，完成后恢复空闲 |
| AC-014 | 后台营销配置 | 模板、签到规则、推广规则可查询和修改 |

### 9.3 部署验收

- `http://127.0.0.1:8087/dashboard/statistics` 返回业务成功；
- `https://域名/api/dashboard/statistics` 经 Nginx 返回同样结果；
- `/user`、`/admin` 及深层路由刷新不返回 404；
- 微信/支付宝启用时，支付与退款回调可通过公网 HTTPS 到达后端。

## 10. 维护约定

功能变更时应同时检查：Vue 菜单和页面、`src/api`、Controller、Service、Entity/Mapper、完整 SQL、增量 SQL、README、需求/API/数据库/原型/项目报告及部署文档。文档中的端口、接口数、表数和状态字典必须以代码和完整 SQL 为准。
